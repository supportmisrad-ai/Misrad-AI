generator client {
  provider = "prisma-client-js"
}

enum MisradHealthStatus {
  CRITICAL
  AT_RISK
  STABLE
  THRIVING
}

enum MisradSentiment {
  POSITIVE
  NEUTRAL
  NEGATIVE
  ANXIOUS
}

enum MisradClientStatus {
  ACTIVE
  LEAD
  ARCHIVED
  LOST
  CHURNED
}

enum MisradClientType {
  RETAINER
  PROJECT
  HOURLY
}

enum MisradJourneyStageStatus {
  COMPLETED
  ACTIVE
  PENDING
}

enum MisradOpportunityStatus {
  NEW
  PROPOSED
  CLOSED
}

enum MisradSuccessGoalStatus {
  IN_PROGRESS
  ACHIEVED
  AT_RISK
}

enum MisradClientActionType {
  APPROVAL
  UPLOAD
  SIGNATURE
  FORM
  FEEDBACK
}

enum MisradClientActionStatus {
  PENDING
  COMPLETED
  OVERDUE
}

enum MisradAssignedFormStatus {
  SENT
  OPENED
  IN_PROGRESS
  COMPLETED
}

enum MisradFeedbackSentiment {
  POSITIVE
  NEUTRAL
  NEGATIVE
}

enum MisradFeedbackSource {
  EMAIL_SURVEY
  PLATFORM_POPUP
  WHATSAPP_BOT
  EXIT_INTERVIEW
  PORTAL_FRICTION
}

enum MisradRoiCategory {
  REVENUE_LIFT
  COST_SAVING
  EFFICIENCY
  REFUND
}

enum MisradAssetType {
  PDF
  IMAGE
  LINK
  FIGMA
  DOC
}

enum MisradAssetCategory {
  BRANDING
  LEGAL
  INPUT
  STRATEGY
}

enum MisradUploadedBy {
  CLIENT
  AGENCY
}

enum MisradDeliverableType {
  CAMPAIGN
  REPORT
  DESIGN
  STRATEGY
  DEV
}

enum MisradDeliverableStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  PUBLISHED
}

enum MisradStakeholderRole {
  CHAMPION
  DECISION_MAKER
  INFLUENCER
  BLOCKER
  GATEKEEPER
  USER
}

enum MisradGroupEventType {
  WEBINAR
  WORKSHOP
  MASTERCLASS
}

enum MisradGroupEventStatus {
  UPCOMING
  COMPLETED
}

enum MisradInvoiceStatus {
  PAID
  PENDING
  OVERDUE
}

enum MisradAgreementType {
  MSA
  SOW
  NDA
  ADDENDUM
}

enum MisradAgreementStatus {
  ACTIVE
  EXPIRED
}

enum MisradCycleStatus {
  RECRUITING
  ACTIVE
  COMPLETED
  CANCELLED
}

enum MisradMeetingLocation {
  ZOOM
  FRONTAL
  PHONE
}

enum MisradMeetingFileType {
  PDF
  DOC
  IMG
}

enum MisradActivityLogType {
  EMAIL
  MEETING
  DELIVERABLE
  SYSTEM
  FINANCIAL
  STATUS_CHANGE
}

enum MisradTaskPriority {
  HIGH
  NORMAL
  LOW
}

enum MisradTaskStatus {
  PENDING
  COMPLETED
}

enum MisradLiabilityRiskLevel {
  HIGH
  MEDIUM
  LOW
}

enum MisradFieldType {
  TEXT
  TEXTAREA
  SELECT
  UPLOAD
  DATE
  CHECKBOX
  RADIO
}

enum MisradFormCategory {
  ONBOARDING
  FEEDBACK
  STRATEGY
}

enum MisradWorkflowItemType {
  MEETING_ZOOM
  MEETING_FRONTAL
  TASK_CLIENT
  TASK_AGENCY
  FORM_SEND
  CONTENT_DELIVERY
}

enum MisradNotificationType {
  ALERT
  MESSAGE
  SUCCESS
  TASK
  SYSTEM
}

model MisradClient {
  id              String   @id @default(uuid()) @db.Uuid
  organizationId  String   @map("organization_id") @db.Uuid
  customerAccountId String? @map("customer_account_id") @db.Uuid

  name            String
  industry        String
  employeeCount   Int
  logoInitials    String

  healthScore     Int
  healthStatus    MisradHealthStatus
  status          MisradClientStatus
  type            MisradClientType

  tags            String[]
  monthlyRetainer Int
  profitMargin    Int
  lifetimeValue   Int
  hoursLogged     Int
  internalHourlyRate Int
  directExpenses  Int
  profitabilityVerdict String

  lastContact     String
  nextRenewal     String
  mainContact     String
  mainContactRole String

  strengths       String[]
  weaknesses      String[]
  sentimentTrend  MisradSentiment[]

  referralStatus  String

  cancellationDate   String?
  cancellationReason String?
  cancellationNote   String?

  cycleId         String?  @db.Uuid

  healthBreakdown   Json
  engagementMetrics Json

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  journeyStages      MisradJourneyStage[]
  milestones         MisradMilestone[]
  opportunities      MisradOpportunity[]
  successGoals       MisradSuccessGoal[]
  metricHistory      MisradMetricHistory[]
  handoff            MisradClientHandoff?
  roiRecords         MisradRoiRecord[]
  actions            MisradClientAction[]
  assignedForms      MisradAssignedForm[]
  feedbackItems      MisradFeedbackItem[]

  assets             MisradClientAsset[]
  deliverables       MisradClientDeliverable[]
  transformations    MisradClientTransformation[]
  stakeholders       MisradStakeholder[]
  meetings           MisradMeeting[]
  meetingFiles       MisradMeetingFile[]
  emails             MisradEmail[]
  invoices           MisradInvoice[]
  agreements         MisradClientAgreement[]
  activityLogs       MisradActivityLog[]
  notifications      MisradNotification[]
  formResponses      MisradFormResponse[]
  meetingAnalysisResults MisradMeetingAnalysisResult[]

  cycle            MisradCycle? @relation(fields: [cycleId], references: [id], onDelete: SetNull)
  customerAccount  CustomerAccount? @relation(fields: [customerAccountId], references: [id], onDelete: SetNull)

  @@map("misrad_clients")
  @@index([organizationId])
  @@index([customerAccountId])
  @@index([cycleId])
}

model MisradJourneyStage {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  name            String
  status          MisradJourneyStageStatus
  date            String?
  completionPercentage Int?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)
  milestones      MisradMilestone[]

  @@map("misrad_journey_stages")
  @@index([organization_id])
  @@index([client_id])
}

model MisradMilestone {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  stage_id        String   @db.Uuid

  label           String
  isCompleted     Boolean
  required        Boolean

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient       @relation(fields: [client_id], references: [id], onDelete: Cascade)
  stage           MisradJourneyStage @relation(fields: [stage_id], references: [id], onDelete: Cascade)

  @@map("misrad_milestones")
  @@index([organization_id])
  @@index([client_id])
  @@index([stage_id])
}

model MisradOpportunity {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  value           Int
  matchScore      Int
  status          MisradOpportunityStatus

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_opportunities")
  @@index([organization_id])
  @@index([client_id])
}

model MisradSuccessGoal {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  metricCurrent   Float
  metricTarget    Float
  unit            String
  deadline        String
  status          MisradSuccessGoalStatus
  lastUpdated     String
  aiForecast      String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)
  history         MisradMetricHistory[]

  @@map("misrad_success_goals")
  @@index([organization_id])
  @@index([client_id])
}

model MisradMetricHistory {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  goal_id         String   @db.Uuid

  date            String
  value           Float

  created_at      DateTime @default(now())

  client          MisradClient      @relation(fields: [client_id], references: [id], onDelete: Cascade)
  goal            MisradSuccessGoal @relation(fields: [goal_id], references: [id], onDelete: Cascade)

  @@map("misrad_metric_history")
  @@index([organization_id])
  @@index([client_id])
  @@index([goal_id])
}

model MisradClientHandoff {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @unique @db.Uuid

  salesRep        String
  handoffDate     String
  keyPromises     String[]
  mainPainPoint   String
  successDefinition30Days String
  notes           String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_handoffs")
  @@index([organization_id])
}

model MisradRoiRecord {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  date            String
  value           Float
  description     String
  category        MisradRoiCategory

  created_at      DateTime @default(now())

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_roi_records")
  @@index([organization_id])
  @@index([client_id])
  @@index([category])
}

model MisradClientAction {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  description     String
  type            MisradClientActionType
  status          MisradClientActionStatus
  dueDate         String
  isBlocking      Boolean
  lastReminderSent String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_actions")
  @@index([organization_id])
  @@index([client_id])
  @@index([status])
}

model MisradAssignedForm {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  templateId      String
  title           String
  status          MisradAssignedFormStatus
  progress        Int
  dateSent        String
  lastActivity    String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_assigned_forms")
  @@index([organization_id])
  @@index([client_id])
  @@index([templateId])
}

model MisradFeedbackItem {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  clientName      String
  score           Int
  comment         String
  date            String
  keywords        String[]
  sentiment       MisradFeedbackSentiment
  source          MisradFeedbackSource

  created_at      DateTime @default(now())

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_feedback_items")
  @@index([organization_id])
  @@index([client_id])
  @@index([source])
}

model MisradClientAsset {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  name            String
  type            MisradAssetType
  url             String
  category        MisradAssetCategory
  uploadedBy      MisradUploadedBy
  date            String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_assets")
  @@index([organization_id])
  @@index([client_id])
  @@index([category])
}

model MisradClientDeliverable {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  description     String
  type            MisradDeliverableType
  thumbnailUrl    String?
  status          MisradDeliverableStatus
  date            String
  tags            String[]

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_deliverables")
  @@index([organization_id])
  @@index([client_id])
  @@index([status])
}

model MisradClientTransformation {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  metrics         String?
  isPublished     Boolean
  before          Json
  after           Json

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_transformations")
  @@index([organization_id])
  @@index([client_id])
  @@index([isPublished])
}

model MisradStakeholder {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  name            String
  jobTitle        String
  nexusRole       MisradStakeholderRole
  influence       Int
  sentiment       Int
  lastContact     String
  email           String?
  avatarUrl       String?
  notes           String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_stakeholders")
  @@index([organization_id])
  @@index([client_id])
  @@index([nexusRole])
}

model MisradMeeting {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  date            String
  title           String
  location        MisradMeetingLocation
  attendees       String[]

  transcript      String
  summary         String?
  recordingUrl    String?
  manualNotes     String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)
  files           MisradMeetingFile[]
  analysis        MisradMeetingAnalysisResult?

  @@map("misrad_meetings")
  @@index([organization_id])
  @@index([client_id])
}

model MisradMeetingFile {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  meeting_id      String   @db.Uuid

  name            String
  url             String
  type            MisradMeetingFileType

  created_at      DateTime @default(now())

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)
  meeting         MisradMeeting @relation(fields: [meeting_id], references: [id], onDelete: Cascade)

  @@map("misrad_meeting_files")
  @@index([organization_id])
  @@index([client_id])
  @@index([meeting_id])
}

model MisradMeetingAnalysisResult {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  meeting_id      String   @unique @db.Uuid

  summary         String
  sentimentScore  Int

  frictionKeywords String[]
  objections      String[]
  compliments     String[]
  decisions       String[]

  intents         String[]
  stories         String[]
  slang           String[]

  rating          Json

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient  @relation(fields: [client_id], references: [id], onDelete: Cascade)
  meeting         MisradMeeting @relation(fields: [meeting_id], references: [id], onDelete: Cascade)

  aiTasks         MisradAiTask[]
  liabilityRisks  MisradAiLiabilityRisk[]

  @@map("misrad_meeting_analysis_results")
  @@index([organization_id])
  @@index([client_id])
}

model MisradAiTask {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  analysis_id     String   @db.Uuid

  bucket          String
  task            String
  deadline        String
  priority        MisradTaskPriority
  status          MisradTaskStatus

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  analysis        MisradMeetingAnalysisResult @relation(fields: [analysis_id], references: [id], onDelete: Cascade)

  @@map("misrad_ai_tasks")
  @@index([organization_id])
  @@index([client_id])
  @@index([analysis_id])
  @@index([bucket])
}

model MisradAiLiabilityRisk {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid
  analysis_id     String   @db.Uuid

  quote           String
  context         String
  riskLevel       MisradLiabilityRiskLevel

  created_at      DateTime @default(now())

  analysis        MisradMeetingAnalysisResult @relation(fields: [analysis_id], references: [id], onDelete: Cascade)

  @@map("misrad_ai_liability_risks")
  @@index([organization_id])
  @@index([client_id])
  @@index([analysis_id])
  @@index([riskLevel])
}

model MisradEmail {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String?  @db.Uuid

  sender          String
  senderEmail     String
  subject         String
  snippet         String
  body            String
  timestamp       String
  isRead          Boolean
  tags            String[]
  avatarUrl       String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient? @relation(fields: [client_id], references: [id], onDelete: SetNull)

  @@map("misrad_emails")
  @@index([organization_id])
  @@index([client_id])
  @@index([isRead])
}

model MisradMessage {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid

  sender_id       String
  recipient_id    String

  subject         String
  body            String
  read_status     Boolean  @default(false)
  related_project_id String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  attachments     MisradMessageAttachment[]

  @@map("misrad_messages")
  @@index([organization_id])
  @@index([sender_id])
  @@index([recipient_id])
  @@index([read_status])
  @@index([related_project_id])
}

model MisradMessageAttachment {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  message_id      String   @db.Uuid

  name            String
  url             String
  mimeType        String?
  sizeBytes       Int?

  created_at      DateTime @default(now())

  message         MisradMessage @relation(fields: [message_id], references: [id], onDelete: Cascade)

  @@map("misrad_message_attachments")
  @@index([organization_id])
  @@index([message_id])
}

model MisradInvoice {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  number          String
  amount          Int
  date            String
  dueDate         String
  status          MisradInvoiceStatus
  downloadUrl     String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_invoices")
  @@index([organization_id])
  @@index([client_id])
  @@index([status])
}

model MisradClientAgreement {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  title           String
  type            MisradAgreementType
  signedDate      String
  expiryDate      String?
  status          MisradAgreementStatus
  fileUrl         String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_client_agreements")
  @@index([organization_id])
  @@index([client_id])
  @@index([status])
}

model MisradCycle {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid

  name            String
  description     String
  startDate       String
  endDate         String
  status          MisradCycleStatus
  groupLinks      Json

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  clients         MisradClient[]
  sharedTasks     MisradCycleTask[]
  sharedAssets    MisradCycleAsset[]
  groupEvents     MisradGroupEvent[]

  @@map("misrad_cycles")
  @@index([organization_id])
  @@index([status])
}

model MisradCycleTask {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  cycle_id        String   @db.Uuid

  title           String
  description     String
  type            MisradClientActionType
  status          MisradClientActionStatus
  dueDate         String
  isBlocking      Boolean
  lastReminderSent String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  cycle           MisradCycle @relation(fields: [cycle_id], references: [id], onDelete: Cascade)

  @@map("misrad_cycle_tasks")
  @@index([organization_id])
  @@index([cycle_id])
}

model MisradCycleAsset {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  cycle_id        String   @db.Uuid

  name            String
  type            MisradAssetType
  url             String
  category        MisradAssetCategory
  uploadedBy      MisradUploadedBy
  date            String

  created_at      DateTime @default(now())

  cycle           MisradCycle @relation(fields: [cycle_id], references: [id], onDelete: Cascade)

  @@map("misrad_cycle_assets")
  @@index([organization_id])
  @@index([cycle_id])
}

model MisradGroupEvent {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  cycle_id        String?  @db.Uuid

  title           String
  type            MisradGroupEventType
  date            String
  targetSegment   String
  attendeesCount  Int
  link            String
  status          MisradGroupEventStatus
  attendees       Json

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  cycle           MisradCycle? @relation(fields: [cycle_id], references: [id], onDelete: SetNull)

  @@map("misrad_group_events")
  @@index([organization_id])
  @@index([cycle_id])
  @@index([status])
}

model MisradWorkflowBlueprint {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid

  name            String
  description     String
  totalDuration   String
  tags            String[]

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  stages          MisradWorkflowStage[]

  @@map("misrad_workflow_blueprints")
  @@index([organization_id])
}

model MisradWorkflowStage {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  blueprint_id    String   @db.Uuid

  title           String
  duration        String

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  blueprint       MisradWorkflowBlueprint @relation(fields: [blueprint_id], references: [id], onDelete: Cascade)
  items           MisradWorkflowItem[]

  @@map("misrad_workflow_stages")
  @@index([organization_id])
  @@index([blueprint_id])
}

model MisradWorkflowItem {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  stage_id        String   @db.Uuid

  type            MisradWorkflowItemType
  title           String
  description     String
  isAutomated     Boolean
  assetLink       String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  stage           MisradWorkflowStage @relation(fields: [stage_id], references: [id], onDelete: Cascade)

  @@map("misrad_workflow_items")
  @@index([organization_id])
  @@index([stage_id])
}

model MisradFormTemplate {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid

  title           String
  description     String
  isActive        Boolean
  category        MisradFormCategory

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  steps           MisradFormStep[]
  responses       MisradFormResponse[]

  @@map("misrad_form_templates")
  @@index([organization_id])
  @@index([category])
}

model MisradFormStep {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  template_id     String   @db.Uuid

  title           String
  description     String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  template        MisradFormTemplate @relation(fields: [template_id], references: [id], onDelete: Cascade)
  fields          MisradFormField[]

  @@map("misrad_form_steps")
  @@index([organization_id])
  @@index([template_id])
}

model MisradFormField {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  step_id         String   @db.Uuid

  label           String
  type            MisradFieldType
  required        Boolean
  options         String[]
  placeholder     String?
  helperText      String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  step            MisradFormStep @relation(fields: [step_id], references: [id], onDelete: Cascade)

  @@map("misrad_form_fields")
  @@index([organization_id])
  @@index([step_id])
}

model MisradFormResponse {
  id               String   @id @default(uuid()) @db.Uuid
  organization_id  String   @db.Uuid
  client_id        String   @db.Uuid

  template_id      String   @db.Uuid
  assigned_form_id String?  @db.Uuid

  status           MisradAssignedFormStatus
  progress         Int
  answers          Json

  submitted_at     DateTime?
  created_at       DateTime @default(now())
  updated_at       DateTime @updatedAt

  client           MisradClient       @relation(fields: [client_id], references: [id], onDelete: Cascade)
  template         MisradFormTemplate @relation(fields: [template_id], references: [id], onDelete: Cascade)

  @@map("misrad_form_responses")
  @@index([organization_id])
  @@index([client_id])
  @@index([template_id])
  @@index([assigned_form_id])
}

model MisradNotification {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String?  @db.Uuid
  recipient_id    String?  @db.Uuid

  type            MisradNotificationType
  title           String
  message         String
  timestamp       String
  isRead          Boolean  @map("is_read")
  link            String?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  client          MisradClient? @relation(fields: [client_id], references: [id], onDelete: SetNull)

  @@map("misrad_notifications")
  @@index([organization_id])
  @@index([client_id])
  @@index([recipient_id])
  @@index([isRead])
}

model MisradActivityLog {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid
  client_id       String   @db.Uuid

  type            MisradActivityLogType
  description     String
  date            String
  isRisk          Boolean

  created_at      DateTime @default(now())

  client          MisradClient @relation(fields: [client_id], references: [id], onDelete: Cascade)

  @@map("misrad_activity_logs")
  @@index([organization_id])
  @@index([client_id])
  @@index([type])
  @@index([isRisk])
}

model MisradModuleSettings {
  id              String   @id @default(uuid()) @db.Uuid
  organization_id String   @db.Uuid

  cycles       Boolean
  intelligence Boolean
  portals      Boolean
  workflows    Boolean
  feedback     Boolean

  created_at   DateTime @default(now())
  updated_at   DateTime @updatedAt

  @@map("misrad_module_settings")
  @@unique([organization_id])
}

datasource db {
  provider = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model NexusUser {
  id                      String    @id(map: "users_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                    String
  role                    String
  department              String?
  avatar                  String?
  online                  Boolean?  @default(false)
  capacity                Int?      @default(0)
  email                   String?
  phone                   String?
  location                String?
  bio                     String?
  paymentType             String?   @map("payment_type")
  hourlyRate              Decimal?  @map("hourly_rate") @db.Decimal(10, 2)
  monthlySalary           Decimal?  @map("monthly_salary") @db.Decimal(10, 2)
  commissionPct           Int?      @map("commission_pct")
  bonusPerTask            Decimal?  @map("bonus_per_task") @db.Decimal(10, 2)
  accumulatedBonus        Decimal?  @default(0) @map("accumulated_bonus") @db.Decimal(10, 2)
  streakDays              Int?      @default(0) @map("streak_days")
  weeklyScore             Decimal?  @map("weekly_score") @db.Decimal(5, 2)
  pendingReward           Json?     @map("pending_reward")
  targets                 Json?
  notificationPreferences Json?     @map("notification_preferences")
  twoFactorEnabled        Boolean?  @default(false) @map("two_factor_enabled")
  isSuperAdmin            Boolean?  @default(false) @map("is_super_admin")
  billingInfo             Json?     @map("billing_info")
  createdAt               DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt               DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  managerId               String?   @map("manager_id") @db.Uuid
  roleId                  String?   @map("role_id") @db.Uuid
  managed_department      String?
  uiPreferences           Json?     @default("{}") @map("ui_preferences")

  @@index([department], map: "idx_users_department")
  @@index([email], map: "idx_users_email")
  @@index([role], map: "idx_users_role")
  @@map("nexus_users")
}

model NexusClient {
  id              String   @id(map: "clients_pkey") @default(uuid())
  organizationId  String   @map("organization_id") @db.Uuid
  name            String
  companyName     String   @map("company_name")
  avatar          String?
  package         String?
  status          String   @default("Onboarding")
  contactPerson   String   @map("contact_person")
  email           String
  phone           String
  joinedAt        DateTime @default(now()) @map("joined_at")
  assetsFolderUrl String?  @map("assets_folder_url")
  source          String?
  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([companyName], map: "idx_clients_company_name")
  @@index([email], map: "idx_clients_email")
  @@index([organizationId], map: "idx_nexus_clients_organization_id")
  @@map("nexus_clients")
}

model NexusTask {
  id                String    @id(map: "tasks_pkey") @default(uuid())
  organizationId     String    @map("organization_id") @db.Uuid
  title             String
  description       String?
  status            String
  priority          String?
  assigneeIds       String[]  @default([]) @map("assignee_ids")
  assigneeId        String?   @map("assignee_id")
  creatorId         String?   @map("creator_id")
  tags              String[]  @default([])
  createdAt         DateTime  @default(now()) @map("created_at")
  dueDate           DateTime? @map("due_date") @db.Date
  dueTime           DateTime? @map("due_time") @db.Time(6)
  timeSpent         Int       @default(0) @map("time_spent")
  estimatedTime     Int?      @map("estimated_time")
  approvalStatus    String?   @map("approval_status")
  isTimerRunning    Boolean   @default(false) @map("is_timer_running")
  messages          Json      @default("[]")
  clientId          String?   @map("client_id")
  isPrivate         Boolean   @default(false) @map("is_private")
  audioUrl          String?   @map("audio_url")
  snoozeCount       Int       @default(0) @map("snooze_count")
  isFocus           Boolean   @default(false) @map("is_focus")
  completionDetails Json?     @map("completion_details")
  department        String?
  updatedAt         DateTime  @updatedAt @map("updated_at")

  organization      social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([assigneeId], map: "idx_tasks_assignee_id")
  @@index([clientId], map: "idx_tasks_client_id")
  @@index([createdAt], map: "idx_tasks_created_at")
  @@index([dueDate], map: "idx_tasks_due_date")
  @@index([status], map: "idx_tasks_status")
  @@index([organizationId], map: "idx_nexus_tasks_organization_id")
  @@map("nexus_tasks")
}

model NexusTimeEntry {
  id              String    @id(map: "time_entries_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String   @map("organization_id") @db.Uuid
  userId          String    @map("user_id") @db.Uuid
  startTime       DateTime  @map("start_time") @db.Timestamptz(6)
  endTime         DateTime? @map("end_time") @db.Timestamptz(6)
  date            DateTime  @db.Date
  durationMinutes Int?      @map("duration_minutes")
  voidReason      String?   @map("void_reason")
  voidedBy        String?   @map("voided_by") @db.Uuid
  voidedAt        DateTime? @map("voided_at") @db.Timestamptz(6)
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([date], map: "idx_time_entries_date")
  @@index([startTime], map: "idx_time_entries_start_time")
  @@index([userId], map: "idx_time_entries_user_id")
  @@index([organizationId], map: "idx_nexus_time_entries_organization_id")
  @@map("nexus_time_entries")
}

model NexusTenant {
  id              String    @id(map: "tenants_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name            String
  ownerEmail      String    @map("owner_email")
  subdomain       String    @unique(map: "tenants_subdomain_key")
  plan            String
  status          String?   @default("Provisioning")
  joinedAt        DateTime? @default(now()) @map("joined_at") @db.Timestamptz(6)
  mrr             Decimal?  @default(0) @db.Decimal(10, 2)
  usersCount      Int?      @default(0) @map("users_count")
  logo            String?
  modules         String[]  @default([])
  region          String?
  version         String?
  allowedEmails   String[]  @default([]) @map("allowed_emails")
  requireApproval Boolean?  @default(false) @map("require_approval")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([ownerEmail], map: "idx_tenants_owner_email")
  @@index([status], map: "idx_tenants_status")
  @@index([subdomain], map: "idx_tenants_subdomain")
  @@map("nexus_tenants")
}

model ClientClient {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  fullName        String    @map("full_name")
  phone           String?   @map("phone")
  email           String?   @map("email")
  notes           String?   @map("notes")
  metadata        Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  profile         ClientProfile?
  internalNotes   ClientInternalNote[]
  serviceTier     ClientServiceTier?
  portalUsers     ClientPortalUser[]
  portalInvites   ClientPortalInvite[]
  sharedFiles     ClientSharedFile[]
  documents       ClientDocument[]
  documentFiles   ClientDocumentFile[]
  approvals       ClientApproval[]
  tasks           ClientTask[]
  sessions        ClientSession[]
  portalContent   ClientPortalContent[]
  feedbacks       ClientFeedback[]

  @@index([organizationId], map: "idx_client_clients_organization_id")
  @@index([fullName], map: "idx_client_clients_full_name")
  @@map("client_clients")
}

model ClientProfile {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @unique @map("client_id") @db.Uuid
  status          String    @default("ACTIVE")
  classification  String?   @default("DEFAULT")
  preferences     Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_profiles_organization_id")
  @@map("client_profiles")
}

model ClientInternalNote {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  body            String
  createdBy       String?   @map("created_by")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_internal_notes_organization_id")
  @@index([clientId], map: "idx_client_internal_notes_client_id")
  @@index([createdAt], map: "idx_client_internal_notes_created_at")
  @@map("client_internal_notes")
}

model ClientServiceTier {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @unique @map("client_id") @db.Uuid
  tierKey         String    @map("tier_key")
  label           String
  metadata        Json?     @default("{}")
  startsAt        DateTime? @map("starts_at") @db.Timestamptz(6)
  endsAt          DateTime? @map("ends_at") @db.Timestamptz(6)
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_service_tiers_organization_id")
  @@index([tierKey], map: "idx_client_service_tiers_tier_key")
  @@map("client_service_tiers")
}

model ClientPortalUser {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  clerkUserId     String    @map("clerk_user_id")
  email           String?
  fullName        String?   @map("full_name")
  status          String    @default("ACTIVE")
  lastLoginAt     DateTime? @map("last_login_at") @db.Timestamptz(6)
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@unique([organizationId, clientId, clerkUserId], map: "uq_client_portal_users_org_client_clerk")
  @@index([organizationId], map: "idx_client_portal_users_organization_id")
  @@index([clientId], map: "idx_client_portal_users_client_id")
  @@index([clerkUserId], map: "idx_client_portal_users_clerk_user_id")
  @@map("client_portal_users")
}

model ClientPortalInvite {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  email           String
  tokenHash       String    @map("token_hash")
  expiresAt       DateTime  @map("expires_at") @db.Timestamptz(6)
  usedAt          DateTime? @map("used_at") @db.Timestamptz(6)
  createdBy       String?   @map("created_by")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_portal_invites_organization_id")
  @@index([clientId], map: "idx_client_portal_invites_client_id")
  @@index([email], map: "idx_client_portal_invites_email")
  @@index([expiresAt], map: "idx_client_portal_invites_expires_at")
  @@map("client_portal_invites")
}

model ClientSharedFile {
  id                String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId    String    @map("organization_id") @db.Uuid
  clientId          String    @map("client_id") @db.Uuid
  title             String?
  description       String?
  storageBucket     String    @default("client") @map("storage_bucket")
  storagePath       String    @map("storage_path")
  mimeType          String?   @map("mime_type")
  sizeBytes         Int?      @map("size_bytes")
  isVisibleToClient Boolean   @default(true) @map("is_visible_to_client")
  uploadedByType    String?   @map("uploaded_by_type")
  uploadedById      String?   @map("uploaded_by_id")
  metadata          Json?     @default("{}")
  createdAt         DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization      social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client            ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_shared_files_organization_id")
  @@index([clientId], map: "idx_client_shared_files_client_id")
  @@index([isVisibleToClient], map: "idx_client_shared_files_visible")
  @@map("client_shared_files")
}

model ClientDocument {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  title           String
  description     String?
  category        String?   @default("GENERAL")
  status          String    @default("DRAFT")
  isVisibleToClient Boolean @default(true) @map("is_visible_to_client")
  metadata        Json?     @default("{}")
  createdByType   String?   @map("created_by_type")
  createdById     String?   @map("created_by_id")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)
  files           ClientDocumentFile[]
  approvals       ClientApproval[]

  @@index([organizationId], map: "idx_client_documents_organization_id")
  @@index([clientId], map: "idx_client_documents_client_id")
  @@index([status], map: "idx_client_documents_status")
  @@index([isVisibleToClient], map: "idx_client_documents_visible")
  @@map("client_documents")
}

model ClientDocumentFile {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  documentId      String    @map("document_id") @db.Uuid
  storageBucket   String    @default("client") @map("storage_bucket")
  storagePath     String    @map("storage_path")
  fileName        String?   @map("file_name")
  mimeType        String?   @map("mime_type")
  sizeBytes       Int?      @map("size_bytes")
  uploadedByType  String?   @map("uploaded_by_type")
  uploadedById    String?   @map("uploaded_by_id")
  metadata        Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)
  document        ClientDocument       @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_document_files_organization_id")
  @@index([clientId], map: "idx_client_document_files_client_id")
  @@index([documentId], map: "idx_client_document_files_document_id")
  @@map("client_document_files")
}

model ClientApproval {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  subjectType     String    @map("subject_type")
  subjectId       String    @map("subject_id")
  documentId      String?   @map("document_id") @db.Uuid
  status          String    @default("PENDING")
  requestedByType String?   @map("requested_by_type")
  requestedById   String?   @map("requested_by_id")
  approvedByPortalUserId String? @map("approved_by_portal_user_id") @db.Uuid
  approvedAt      DateTime? @map("approved_at") @db.Timestamptz(6)
  comment         String?
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient         @relation(fields: [clientId], references: [id], onDelete: Cascade)
  document        ClientDocument?      @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_approvals_organization_id")
  @@index([clientId], map: "idx_client_approvals_client_id")
  @@index([status], map: "idx_client_approvals_status")
  @@index([subjectType, subjectId], map: "idx_client_approvals_subject")
  @@map("client_approvals")
}

model ClientTask {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  title           String
  description     String?
  status          String    @default("todo")
  priority        String?   @default("medium")
  dueAt           DateTime? @map("due_at") @db.Timestamptz(6)
  assignedTo      String?   @map("assigned_to") @db.Uuid
  createdBy       String?   @map("created_by") @db.Uuid
  metadata        Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_tasks_organization_id")
  @@index([clientId], map: "idx_client_tasks_client_id")
  @@index([status], map: "idx_client_tasks_status")
  @@index([dueAt], map: "idx_client_tasks_due_at")
  @@map("client_tasks")
}

model ClientSession {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  startAt         DateTime  @map("start_at") @db.Timestamptz(6)
  endAt           DateTime? @map("end_at") @db.Timestamptz(6)
  status          String    @default("scheduled")
  sessionType     String?   @map("session_type")
  location        String?
  summary         String?
  createdBy       String?   @map("created_by") @db.Uuid
  metadata        Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_sessions_organization_id")
  @@index([clientId], map: "idx_client_sessions_client_id")
  @@index([startAt], map: "idx_client_sessions_start_at")
  @@map("client_sessions")
}

model ClientFeedback {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  rating          Int       @db.SmallInt
  comment         String?
  metadata        Json?     @default("{}")
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_feedbacks_organization_id")
  @@index([clientId], map: "idx_client_feedbacks_client_id")
  @@index([rating], map: "idx_client_feedbacks_rating")
  @@index([createdAt], map: "idx_client_feedbacks_created_at")
  @@map("client_feedbacks")
}

model ClientPortalContent {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  clientId        String    @map("client_id") @db.Uuid
  kind            String
  title           String
  body            String?
  data            Json?     @default("{}")
  isPublished     Boolean?  @default(false) @map("is_published")
  publishedAt     DateTime? @map("published_at") @db.Timestamptz(6)
  createdBy       String?   @map("created_by") @db.Uuid
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  client          ClientClient @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@index([organizationId], map: "idx_client_portal_content_organization_id")
  @@index([clientId], map: "idx_client_portal_content_client_id")
  @@index([kind], map: "idx_client_portal_content_kind")
  @@index([isPublished], map: "idx_client_portal_content_is_published")
  @@map("client_portal_content")
}

model SystemLead {
  id                  String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId       String                 @map("organization_id") @db.Uuid
  customerAccountId    String?                @map("customer_account_id") @db.Uuid
  name                String
  company             String?
  phone               String
  email               String
  source              String
  status              String
  value               Decimal?               @default(0) @db.Decimal(10, 2)
  lastContact         DateTime               @map("last_contact") @db.Timestamptz(6)
  createdAt           DateTime?              @default(now()) @map("created_at") @db.Timestamptz(6)
  isHot               Boolean?               @default(false) @map("is_hot")
  score               Int?                   @default(0)
  address             String?
  locationX           Decimal?               @map("location_x") @db.Decimal(10, 6)
  locationY           Decimal?               @map("location_y") @db.Decimal(10, 6)
  assignedAgentId     String?                @map("assigned_agent_id") @db.Uuid
  subscriptionEndDate DateTime?              @map("subscription_end_date") @db.Timestamptz(6)
  productInterest     String?                @map("product_interest")
  playbookStep        String?                @map("playbook_step")
  budgetHours         Int?                   @map("budget_hours")
  loggedHours         Int?                   @map("logged_hours")
  aiTags              Json?                  @default("[]") @map("ai_tags")
  updatedAt           DateTime?              @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  calendarEvents      SystemCalendarEvent[]
  callAnalyses        SystemCallAnalysis[]
  invoices            SystemInvoice[]
  activities          SystemLeadActivity[]
  handover            SystemLeadHandover?
  portalApprovals     SystemPortalApproval[]
  portalTasks         SystemPortalTask[]
  supportTickets      SystemSupportTicket[]

  organization        social_organizations   @relation(fields: [organizationId], references: [id], onDelete: Restrict)
  customerAccount     CustomerAccount?       @relation(fields: [customerAccountId], references: [id], onDelete: SetNull)

  @@index([assignedAgentId], map: "idx_system_leads_assigned_agent_id")
  @@index([organizationId], map: "idx_system_leads_organization_id")
  @@index([customerAccountId], map: "idx_system_leads_customer_account_id")
  @@index([createdAt], map: "idx_system_leads_created_at")
  @@index([isHot], map: "idx_system_leads_is_hot")
  @@index([status], map: "idx_system_leads_status")
  @@map("system_leads")
}

model SystemLeadActivity {
  id        String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId    String     @map("lead_id") @db.Uuid
  type      String
  content   String
  timestamp DateTime?  @default(now()) @db.Timestamptz(6)
  direction String?
  metadata  Json?
  createdAt DateTime?  @default(now()) @map("created_at") @db.Timestamptz(6)
  lead      SystemLead @relation(fields: [leadId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([leadId], map: "idx_system_lead_activities_lead_id")
  @@index([timestamp], map: "idx_system_lead_activities_timestamp")
  @@map("system_lead_activities")
}

model SystemLeadHandover {
  id                  String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId              String     @unique @map("lead_id") @db.Uuid
  nonStandardPromises String     @map("non_standard_promises")
  biggestPain         String     @map("biggest_pain")
  expectations30Days  String     @map("expectations_30_days")
  filledAt            DateTime?  @default(now()) @map("filled_at") @db.Timestamptz(6)
  createdAt           DateTime?  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime?  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead                SystemLead @relation(fields: [leadId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("system_lead_handovers")
}

model SystemTask {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  title       String
  description String?
  assigneeId  String    @map("assignee_id") @db.Uuid
  dueDate     DateTime  @map("due_date") @db.Timestamptz(6)
  priority    String
  status      String
  tags        Json?     @default("[]")
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([assigneeId], map: "idx_system_tasks_assignee_id")
  @@index([dueDate], map: "idx_system_tasks_due_date")
  @@index([status], map: "idx_system_tasks_status")
  @@map("system_tasks")
}

model SystemInvoice {
  id           String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId       String?     @map("lead_id") @db.Uuid
  client       String
  amount       Decimal     @db.Decimal(10, 2)
  date         DateTime?   @default(now()) @db.Timestamptz(6)
  status       String
  item         String
  refundReason String?     @map("refund_reason")
  createdAt    DateTime?   @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime?   @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead         SystemLead? @relation(fields: [leadId], references: [id], onUpdate: NoAction)

  @@index([date], map: "idx_system_invoices_date")
  @@index([leadId], map: "idx_system_invoices_lead_id")
  @@index([status], map: "idx_system_invoices_status")
  @@map("system_invoices")
}

model SystemCalendarEvent {
  id           String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId       String?     @map("lead_id") @db.Uuid
  title        String
  leadName     String      @map("lead_name")
  leadCompany  String      @map("lead_company")
  dayName      String      @map("day_name")
  date         String
  time         String
  type         String
  location     String
  participants Int?
  reminders    Json?
  postMeeting  Json?       @map("post_meeting")
  createdAt    DateTime?   @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime?   @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead         SystemLead? @relation(fields: [leadId], references: [id], onUpdate: NoAction)

  @@index([date], map: "idx_system_calendar_events_date")
  @@index([leadId], map: "idx_system_calendar_events_lead_id")
  @@map("system_calendar_events")
}

model SystemCampaign {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name        String
  platform    String
  status      String
  budget      Decimal?  @default(0) @db.Decimal(10, 2)
  spent       Decimal?  @default(0) @db.Decimal(10, 2)
  leads       Int?      @default(0)
  cpl         Decimal?  @default(0) @db.Decimal(10, 2)
  roas        Decimal?  @default(0) @db.Decimal(10, 2)
  impressions Int?      @default(0)
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([createdAt], map: "idx_system_campaigns_created_at")
  @@index([status], map: "idx_system_campaigns_status")
  @@map("system_campaigns")
}

model SystemStudent {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name        String
  company     String
  avatar      String
  progress    Int?      @default(0)
  status      String
  cohort      String
  lastCheckIn DateTime  @map("last_check_in") @db.Timestamptz(6)
  nextHotSeat DateTime? @map("next_hot_seat") @db.Timestamptz(6)
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([cohort], map: "idx_system_students_cohort")
  @@index([status], map: "idx_system_students_status")
  @@map("system_students")
}

model SystemContentItem {
  id        String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  title     String
  format    String
  platform  String
  status    String
  assignee  String
  views     Int?      @default(0)
  createdAt DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([platform], map: "idx_system_content_items_platform")
  @@index([status], map: "idx_system_content_items_status")
  @@map("system_content_items")
}

model SystemFieldAgent {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  userId      String?   @map("user_id") @db.Uuid
  name        String
  status      String
  locationX   Decimal   @map("location_x") @db.Decimal(10, 6)
  locationY   Decimal   @map("location_y") @db.Decimal(10, 6)
  avatar      String
  phone       String
  area        String
  currentTask String?   @map("current_task")
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([status], map: "idx_system_field_agents_status")
  @@index([userId], map: "idx_system_field_agents_user_id")
  @@map("system_field_agents")
}

model SystemWebhookLog {
  id        String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  timestamp DateTime? @default(now()) @db.Timestamptz(6)
  channel   String
  event     String
  status    String

  @@index([channel], map: "idx_system_webhook_logs_channel")
  @@index([timestamp], map: "idx_system_webhook_logs_timestamp")
  @@map("system_webhook_logs")
}

model SystemCallAnalysis {
  id         String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId     String?     @map("lead_id") @db.Uuid
  fileName   String      @map("file_name")
  title      String
  audioUrl   String      @map("audio_url")
  date       String
  duration   String
  summary    String
  score      Int?        @default(0)
  intent     String
  userNotes  String?     @map("user_notes")
  transcript Json?       @default("[]")
  topics     Json?       @default("{}")
  feedback   Json?       @default("{}")
  createdAt  DateTime?   @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt  DateTime?   @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead       SystemLead? @relation(fields: [leadId], references: [id], onUpdate: NoAction)

  @@index([date], map: "idx_system_call_analyses_date")
  @@index([leadId], map: "idx_system_call_analyses_lead_id")
  @@index([score], map: "idx_system_call_analyses_score")
  @@map("system_call_analyses")
}

model SystemAutomation {
  id            String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name          String
  enabled       Boolean?  @default(true)
  triggerType   String    @map("trigger_type")
  triggerStatus String?   @map("trigger_status")
  actionType    String    @map("action_type")
  taskTitle     String?   @map("task_title")
  taskDueDays   Int?      @map("task_due_days")
  taskPriority  String?   @map("task_priority")
  noteText      String?   @map("note_text")
  runs          Int?      @default(0)
  lastRun       DateTime? @map("last_run") @db.Timestamptz(6)
  createdAt     DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt     DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([enabled], map: "idx_system_automations_enabled")
  @@index([triggerType], map: "idx_system_automations_trigger_type")
  @@map("system_automations")
}

model SystemForm {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  title       String
  status      String
  views       Int?      @default(0)
  conversions Int?      @default(0)
  fields      Json?     @default("[]")
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([createdAt], map: "idx_system_forms_created_at")
  @@index([status], map: "idx_system_forms_status")
  @@map("system_forms")
}

model SystemPartner {
  id               String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name             String
  type             String
  referrals        Int?      @default(0)
  revenue          Decimal?  @default(0) @db.Decimal(10, 2)
  commissionRate   Decimal?  @default(0) @map("commission_rate") @db.Decimal(5, 2)
  unpaidCommission Decimal?  @default(0) @map("unpaid_commission") @db.Decimal(10, 2)
  lastActive       String    @map("last_active")
  avatar           String
  status           String
  createdAt        DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([status], map: "idx_system_partners_status")
  @@index([type], map: "idx_system_partners_type")
  @@map("system_partners")
}

model SystemAsset {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  title       String
  description String?
  category    String
  type        String
  value       String
  isSecure    Boolean?  @default(false) @map("is_secure")
  createdAt   DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime? @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([category], map: "idx_system_assets_category")
  @@index([type], map: "idx_system_assets_type")
  @@map("system_assets")
}

model SystemPortalApproval {
  id        String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId    String     @map("lead_id") @db.Uuid
  type      String
  title     String
  date      String
  status    String
  createdAt DateTime?  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime?  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead      SystemLead @relation(fields: [leadId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([leadId], map: "idx_system_portal_approvals_lead_id")
  @@index([status], map: "idx_system_portal_approvals_status")
  @@map("system_portal_approvals")
}

model SystemPortalTask {
  id         String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId     String     @map("lead_id") @db.Uuid
  title      String
  type       String
  status     String
  formFields Json?      @default("[]") @map("form_fields")
  createdAt  DateTime?  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt  DateTime?  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead       SystemLead @relation(fields: [leadId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([leadId], map: "idx_system_portal_tasks_lead_id")
  @@index([status], map: "idx_system_portal_tasks_status")
  @@map("system_portal_tasks")
}

model SystemSupportTicket {
  id        String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leadId    String     @map("lead_id") @db.Uuid
  category  String
  subject   String
  status    String
  createdAt DateTime?  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime?  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  lead      SystemLead @relation(fields: [leadId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([category], map: "idx_system_support_tickets_category")
  @@index([leadId], map: "idx_system_support_tickets_lead_id")
  @@index([status], map: "idx_system_support_tickets_status")
  @@map("system_support_tickets")
}

model SocialPost {
  id                     String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  clientId               String                   @map("client_id") @db.Uuid
  content                String
  media_url              String?
  status                 String?                  @default("draft")
  scheduled_at           DateTime?                @db.Timestamptz(6)
  published_at           DateTime?                @db.Timestamptz(6)
  is_late                Boolean?                 @default(false)
  created_by             String?                  @db.Uuid
  createdAt              DateTime?                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime?                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deleted_at             DateTime?                @db.Timestamptz(6)
  deleted_by             String?                  @db.Uuid
  social_post_comments   social_post_comments[]
  social_post_platforms  social_post_platforms[]
  social_post_variations social_post_variations[]

  @@index([clientId], map: "idx_social_posts_client_id")
  @@index([status], map: "idx_social_posts_status")
  @@map("social_posts")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nexus_employee_invitation_links {
  id             String    @id(map: "employee_invitation_links_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  token          String    @unique(map: "employee_invitation_links_token_key")
  created_by     String?   @db.Uuid
  department     String?
  role           String?
  employee_email String?
  employee_name  String?
  employee_phone String?
  payment_type   String?
  hourly_rate    Decimal?  @db.Decimal(10, 2)
  monthly_salary Decimal?  @db.Decimal(10, 2)
  commission_pct Int?
  start_date     DateTime? @db.Date
  notes          String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  expires_at     DateTime? @db.Timestamptz(6)
  used_at        DateTime? @db.Timestamptz(6)
  is_used        Boolean?  @default(false)
  is_active      Boolean?  @default(true)
  metadata       Json?     @default("{}")
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)

  organization   social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([token], map: "idx_employee_invitation_links_token")
  @@index([organizationId], map: "idx_nexus_employee_invitation_links_organization_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nexus_event_attendance {
  id          String    @id(map: "event_attendance_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId String @map("organization_id") @db.Uuid
  event_id    String    @db.Uuid
  user_id     String    @db.Uuid
  status      String?   @default("invited")
  rsvp_at     DateTime? @db.Timestamptz(6)
  attended_at DateTime? @db.Timestamptz(6)
  notes       String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  organization social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@unique([event_id, user_id], map: "event_attendance_event_id_user_id_key")
  @@index([event_id], map: "idx_event_attendance_event")
  @@index([organizationId], map: "idx_nexus_event_attendance_organization_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nexus_leave_requests {
  id                String    @id(map: "leave_requests_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId     String   @map("organization_id") @db.Uuid
  tenant_id         String?   @db.Uuid
  employee_id       String    @db.Uuid
  leave_type        String
  start_date        DateTime  @db.Date
  end_date          DateTime  @db.Date
  days_requested    Decimal   @db.Decimal(5, 2)
  reason            String?
  status            String?   @default("pending")
  requested_by      String?   @db.Uuid
  approved_by       String?   @db.Uuid
  approved_at       DateTime? @db.Timestamptz(6)
  rejection_reason  String?
  notification_sent Boolean?  @default(false)
  employee_notified Boolean?  @default(false)
  metadata          Json?     @default("{}")
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  updated_at        DateTime? @default(now()) @db.Timestamptz(6)

  organization       social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([employee_id], map: "idx_leave_requests_employee")
  @@index([organizationId], map: "idx_nexus_leave_requests_organization_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nexus_team_events {
  id                   String    @id(map: "team_events_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  organizationId        String   @map("organization_id") @db.Uuid
  tenant_id            String?   @db.Uuid
  title                String
  description          String?
  event_type           String
  start_date           DateTime  @db.Timestamptz(6)
  end_date             DateTime  @db.Timestamptz(6)
  all_day              Boolean?  @default(false)
  location             String?
  organizer_id         String?   @db.Uuid
  required_attendees   String[]  @default([]) @db.Uuid
  optional_attendees   String[]  @default([]) @db.Uuid
  status               String?   @default("scheduled")
  requires_approval    Boolean?  @default(false)
  approved_by          String?   @db.Uuid
  approved_at          DateTime? @db.Timestamptz(6)
  notification_sent    Boolean?  @default(false)
  reminder_sent        Boolean?  @default(false)
  reminder_days_before Int?      @default(1)
  metadata             Json?     @default("{}")
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  created_by           String?   @db.Uuid
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)

  organization          social_organizations @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  @@index([tenant_id], map: "idx_team_events_tenant")
  @@index([organizationId], map: "idx_nexus_team_events_organization_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scale_calendar_sync_log {
  id             String    @id(map: "calendar_sync_log_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  integration_id String?   @db.Uuid
  event_id       String
  action         String
  direction      String
  status         String
  error_message  String?
  metadata       Json?     @default("{}")
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@index([integration_id], map: "idx_calendar_sync_log_integration_id")
  @@map("misrad_calendar_sync_log")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scale_feature_requests {
  id               String    @id(map: "feature_requests_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String    @db.Uuid
  tenant_id        String?   @db.Uuid
  title            String
  description      String
  type             String
  priority         String    @default("medium")
  status           String    @default("pending")
  votes            Json?     @default("[]")
  assigned_to      String?   @db.Uuid
  reviewed_by      String?   @db.Uuid
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  reviewed_at      DateTime? @db.Timestamptz(6)
  completed_at     DateTime? @db.Timestamptz(6)
  metadata         Json?     @default("{}")
  admin_notes      String?
  rejection_reason String?

  @@map("misrad_feature_requests")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scale_integrations {
  id             String    @id(map: "integrations_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id        String?   @db.Uuid
  tenant_id      String?   @db.Uuid
  service_type   String
  access_token   String
  refresh_token  String?
  token_type     String?   @default("Bearer")
  expires_at     DateTime? @db.Timestamptz(6)
  scope          String[]
  metadata       Json?     @default("{}")
  is_active      Boolean?  @default(true)
  last_synced_at DateTime? @db.Timestamptz(6)
  sync_error     String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([user_id, tenant_id, service_type], map: "integrations_user_id_tenant_id_service_type_key")
  @@index([service_type], map: "idx_integrations_service_type")
  @@index([tenant_id], map: "idx_integrations_tenant_id")
  @@index([user_id], map: "idx_integrations_user_id")
  @@map("misrad_integrations")
}

model scale_notifications {
  id           String    @id(map: "notifications_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  recipient_id String?   @db.Uuid
  type         String
  text         String
  actor_id     String?   @db.Uuid
  actor_name   String?
  related_id   String?   @db.Uuid
  is_read      Boolean?  @default(false)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)

  @@index([is_read], map: "idx_notifications_is_read")
  @@index([recipient_id], map: "idx_notifications_recipient_id")
  @@map("scale_notifications")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scale_permissions {
  id          String    @id(map: "permissions_pkey")
  label       String
  description String?
  category    String?   @default("access")
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@map("misrad_permissions")
}

model scale_roles {
  id          String    @id(map: "roles_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name        String    @unique(map: "roles_name_key")
  description String?
  is_system   Boolean?  @default(false)
  permissions String[]  @default([])
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@index([name], map: "idx_roles_name")
  @@map("misrad_roles")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scale_support_tickets {
  id               String    @id(map: "support_tickets_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String    @db.Uuid
  tenant_id        String?   @db.Uuid
  category         String
  subject          String
  message          String
  ticket_number    String    @unique(map: "support_tickets_ticket_number_key")
  status           String    @default("open")
  priority         String    @default("medium")
  assigned_to      String?   @db.Uuid
  resolved_by      String?   @db.Uuid
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  resolved_at      DateTime? @db.Timestamptz(6)
  closed_at        DateTime? @db.Timestamptz(6)
  metadata         Json?     @default("{}")
  admin_response   String?
  resolution_notes String?

  @@map("misrad_support_tickets")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_activity_logs {
  id                  String              @id(map: "activity_logs_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  team_member_id      String              @db.Uuid
  action              String
  target_id           String?             @db.Uuid
  target_type         String?
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  user_id             String?
  social_team_members social_team_members @relation(fields: [team_member_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "activity_logs_team_member_id_fkey")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_agency_service_configs {
  id           String    @id(map: "agency_service_configs_pkey")
  label        String
  is_enabled   Boolean?  @default(true)
  base_price   Decimal   @db.Decimal(10, 2)
  category     String?
  description  String?
  is_recurring Boolean?  @default(false)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_ai_opportunities {
  id            String    @id(map: "ai_opportunities_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id     String    @db.Uuid
  title         String
  description   String
  type          String
  draft_content String?
  media_url     String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_app_updates {
  id                       String                     @id(map: "app_updates_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version                  String
  title                    String
  description              String
  category                 String
  priority                 String?                    @default("medium")
  is_published             Boolean?                   @default(true)
  published_at             DateTime?                  @default(now()) @db.Timestamptz(6)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  social_user_update_views social_user_update_views[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_automation_rules {
  id           String    @id(map: "automation_rules_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id    String?   @db.Uuid
  type         String
  trigger_days Int
  is_enabled   Boolean?  @default(true)
  title        String
  description  String
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_business_metrics {
  id                     String    @id(map: "business_metrics_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id              String    @unique(map: "business_metrics_client_id_key") @db.Uuid
  time_spent_minutes     Int?      @default(0)
  expected_hours         Decimal?  @default(0) @db.Decimal(5, 2)
  punctuality_score      Int?      @default(0)
  responsiveness_score   Int?      @default(0)
  revision_count         Int?      @default(0)
  last_ai_business_audit String?
  days_overdue           Int?      @default(0)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  updated_at             DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_calendar_events {
  id              String    @id(map: "calendar_events_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id         String?   @db.Uuid
  client_id       String?   @db.Uuid
  google_event_id String?
  title           String
  description     String?
  start_time      DateTime  @db.Timestamptz(6)
  end_time        DateTime? @db.Timestamptz(6)
  calendar_id     String?
  is_all_day      Boolean?  @default(false)
  location        String?
  attendees       String[]
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([google_event_id, user_id], map: "calendar_events_google_event_id_user_id_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_campaigns {
  id          String    @id(map: "campaigns_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id   String    @db.Uuid
  name        String
  status      String?   @default("draft")
  objective   String?   @default("awareness")
  budget      Decimal   @db.Decimal(10, 2)
  spent       Decimal?  @default(0) @db.Decimal(10, 2)
  roas        Decimal?  @default(0) @db.Decimal(5, 2)
  impressions Int?      @default(0)
  clicks      Int?      @default(0)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@index([client_id], map: "idx_campaigns_client_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_client_active_platforms {
  client_id String @db.Uuid
  platform  String

  @@id([client_id, platform], map: "client_active_platforms_pkey")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_client_dna {
  id                   String    @id(map: "client_dna_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id            String    @unique(map: "client_dna_client_id_key") @db.Uuid
  brand_summary        String?
  voice_formal         Int?
  voice_funny          Int?
  voice_length         Int?
  vocabulary_loved     Json?     @default("[]")
  vocabulary_forbidden Json?     @default("[]")
  color_primary        String?
  color_secondary      String?
  target_audience      String?
  pain_points          String?
  unique_value         String?
  competitors          String?
  main_goal            String?
  ai_strategy_summary  String?
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)

  @@index([client_id], map: "idx_client_dna_client_id")
  @@index([vocabulary_forbidden], map: "idx_client_dna_vocabulary_forbidden", type: Gin)
  @@index([vocabulary_loved], map: "idx_client_dna_vocabulary_loved", type: Gin)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_client_requests {
  id              String    @id(map: "client_requests_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id       String    @db.Uuid
  type            String
  content         String
  media_url       String?
  status          String?   @default("new")
  manager_comment String?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_clients {
  id                     String                @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id                String?               @db.Uuid
  name                   String
  company_name           String
  business_id            String?
  phone                  String?
  email                  String?
  avatar                 String?
  brand_voice            String?
  posting_rhythm         String?
  status                 String?               @default("Onboarding")
  onboarding_status      String?
  invitation_token       String?
  portal_token           String                @unique(map: "clients_portal_token_key")
  color                  String?
  plan                   String?
  monthly_fee            Decimal?              @db.Decimal(10, 2)
  next_payment_date      DateTime?             @db.Date
  next_payment_amount    Decimal?              @db.Decimal(10, 2)
  payment_status         String?
  auto_reminders_enabled Boolean?              @default(false)
  saved_card_thumbnail   String?
  internal_notes         String?
  created_at             DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime?             @db.Timestamptz(6)
  deleted_by             String?               @db.Uuid
  organization_id        String?               @db.Uuid
  social_organizations   social_organizations? @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "clients_organization_id_fkey")

  @@index([invitation_token], map: "clients_invitationToken_idx")
  @@index([organization_id], map: "clients_organization_id_idx")
  @@index([plan], map: "idx_clients_plan")
  @@index([status], map: "idx_clients_status")
  @@index([user_id], map: "idx_clients_user_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_conversations {
  id              String            @id(map: "conversations_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id       String            @db.Uuid
  platform        String
  user_name       String
  user_avatar     String?
  last_message    String?
  unread_count    Int?              @default(0)
  created_at      DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?         @default(now()) @db.Timestamptz(6)
  social_messages social_messages[]

  @@index([client_id], map: "idx_conversations_client_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_drive_files {
  id             String    @id(map: "drive_files_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id        String?   @db.Uuid
  client_id      String?   @db.Uuid
  google_file_id String
  name           String
  mime_type      String?
  size_bytes     BigInt?
  folder_path    String?
  web_view_link  String?
  thumbnail_link String?
  synced_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([google_file_id, user_id], map: "drive_files_google_file_id_user_id_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_feedback {
  id         String    @id(map: "feedback_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String?   @db.Uuid
  client_id  String?   @db.Uuid
  type       String?   @default("other")
  title      String
  message    String
  status     String?   @default("new")
  priority   String?   @default("medium")
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_global_system_metrics {
  id                     String    @id(map: "global_system_metrics_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  total_mrr              Decimal?  @default(0) @db.Decimal(12, 2)
  active_subscriptions   Int?      @default(0)
  overdue_invoices_count Int?      @default(0)
  api_health_score       Int?      @default(100)
  gemini_token_usage     Int?      @default(0)
  new_clients_this_month Int?      @default(0)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  updated_at             DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_ideas {
  id         String    @id(map: "ideas_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id  String    @db.Uuid
  text       String
  created_at DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_impersonation_sessions {
  id            String    @id(map: "impersonation_sessions_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  admin_user_id String    @db.Uuid
  client_id     String    @db.Uuid
  token         String    @unique(map: "impersonation_sessions_token_key")
  expires_at    DateTime  @db.Timestamptz(6)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_integration_credentials {
  id               String    @id(map: "integration_credentials_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String?   @db.Uuid
  integration_name String
  credential_type  String
  encrypted_data   Json
  metadata         Json?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([user_id, integration_name], map: "integration_credentials_user_id_integration_name_key")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_integration_status {
  id           String    @id(map: "integration_status_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name         String    @unique(map: "integration_status_name_key")
  is_connected Boolean?  @default(false)
  api_key      String?
  last_sync    DateTime? @db.Timestamptz(6)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_invoices {
  id             String    @id(map: "invoices_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id      String    @db.Uuid
  invoice_number String    @unique(map: "invoices_invoice_number_key")
  amount         Decimal   @db.Decimal(10, 2)
  date           DateTime  @db.Date
  status         String?   @default("pending")
  download_url   String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@index([client_id], map: "idx_invoices_client_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_manager_requests {
  id                   String    @id(map: "manager_requests_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id            String    @db.Uuid
  title                String
  description          String
  status               String?   @default("pending")
  type                 String
  feedback_from_client String?
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_messages {
  id                   String               @id(map: "messages_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  conversation_id      String               @db.Uuid
  sender               String
  text                 String
  is_me                Boolean?             @default(false)
  created_at           DateTime?            @default(now()) @db.Timestamptz(6)
  social_conversations social_conversations @relation(fields: [conversation_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "messages_conversation_id_fkey")

  @@index([conversation_id], map: "idx_messages_conversation_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_navigation_menu {
  id              String    @id(map: "navigation_menu_pkey")
  label           String
  icon            String
  view            String
  section         String
  order           Int       @default(0)
  is_visible      Boolean?  @default(true)
  requires_client Boolean?  @default(false)
  requires_role   Json?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_notifications {
  id          String    @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id     String?   @db.Uuid
  title       String
  message     String
  type        String?   @default("info")
  is_read     Boolean?  @default(false)
  target_type String?   @default("all")
  target_id   String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  read_at     DateTime? @db.Timestamptz(6)

  @@ignore
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_oauth_tokens {
  id               String    @id(map: "oauth_tokens_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String?   @db.Uuid
  integration_name String
  access_token     String
  refresh_token    String?
  token_type       String?   @default("Bearer")
  expires_at       DateTime? @db.Timestamptz(6)
  scope            String?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([user_id, integration_name], map: "oauth_tokens_user_id_integration_name_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_organizations {
  id                      String                @id(map: "organizations_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                    String
  slug                    String?
  logo                    String?
  owner_id                String                @unique(map: "organizations_owner_id_key") @db.Uuid
  has_nexus                Boolean?              @default(false)
  has_social               Boolean?              @default(false)
  has_system               Boolean?              @default(false)
  has_finance              Boolean?              @default(false)
  has_client               Boolean?              @default(false)
  subscription_status     String?               @default("trial")
  subscription_plan       String?
  trial_start_date        DateTime?             @db.Timestamptz(6)
  trial_days              Int?                  @default(30)
  subscription_start_date DateTime?             @db.Timestamptz(6)
  created_at              DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?             @default(now()) @db.Timestamptz(6)
  social_clients          social_clients[]
  social_team_members     social_team_members[]
  social_users            social_users[]
  customerAccounts        CustomerAccount[]
  systemLeads             SystemLead[]
  nexusClients            NexusClient[]
  nexusTasks              NexusTask[]
  nexusTimeEntries        NexusTimeEntry[]
  clientClients           ClientClient[]
  clientProfiles          ClientProfile[]
  clientInternalNotes     ClientInternalNote[]
  clientServiceTiers      ClientServiceTier[]
  clientPortalUsers       ClientPortalUser[]
  clientPortalInvites     ClientPortalInvite[]
  clientSharedFiles       ClientSharedFile[]
  clientDocuments         ClientDocument[]
  clientDocumentFiles     ClientDocumentFile[]
  clientApprovals         ClientApproval[]
  clientTasks             ClientTask[]
  clientSessions          ClientSession[]
  clientFeedbacks         ClientFeedback[]
  clientPortalContents    ClientPortalContent[]
  nexusEmployeeInvitationLinks nexus_employee_invitation_links[]
  nexusEventAttendance    nexus_event_attendance[]
  nexusLeaveRequests      nexus_leave_requests[]
  nexusTeamEvents         nexus_team_events[]
  organizationFeatures    organization_features[]

  @@index([owner_id], map: "organizations_owner_id_idx")
  @@map("organizations")
}

model organization_features {
  id              String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String               @db.Uuid
  feature_key     String
  enabled         Boolean?             @default(true)
  created_at      DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?            @default(now()) @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade)

  @@unique([organization_id, feature_key], map: "organization_features_org_feature_key")
  @@index([organization_id], map: "organization_features_organization_id_idx")
  @@map("organization_features")
}

model CustomerAccount {
  id              String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId  String              @map("organization_id") @db.Uuid

  name            String
  company_name    String?
  phone           String?
  email           String?

  created_at      DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?           @default(now()) @db.Timestamptz(6)

  organization    social_organizations @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  systemLeads     SystemLead[]
  misradClients   MisradClient[]

  @@map("customer_accounts")
  @@index([organizationId])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_payment_orders {
  id                   String    @id(map: "payment_orders_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id            String    @db.Uuid
  amount               Decimal   @db.Decimal(10, 2)
  description          String?
  status               String?   @default("pending")
  installments_allowed Int?      @default(1)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)

  @@index([client_id], map: "idx_payment_orders_client_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_platform_credentials {
  id         String    @id(map: "platform_credentials_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id  String    @db.Uuid
  platform   String
  username   String?
  notes      String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([client_id, platform], map: "platform_credentials_client_id_platform_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_platform_quotas {
  id            String    @id(map: "platform_quotas_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id     String    @db.Uuid
  platform      String
  monthly_limit Int?      @default(0)
  current_usage Int?      @default(0)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([client_id, platform], map: "platform_quotas_client_id_platform_key")
  @@index([client_id], map: "idx_platform_quotas_client_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_post_comments {
  id                  String              @id(map: "post_comments_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  post_id             String              @db.Uuid
  team_member_id      String              @db.Uuid
  text                String
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  social_posts        SocialPost          @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_comments_post_id_fkey")
  social_team_members social_team_members @relation(fields: [team_member_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_comments_team_member_id_fkey")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_post_platforms {
  post_id      String     @db.Uuid
  platform     String
  social_posts SocialPost @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_platforms_post_id_fkey")

  @@id([post_id, platform], map: "post_platforms_pkey")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_post_variations {
  id               String     @id(map: "post_variations_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  post_id          String     @db.Uuid
  type             String
  content          String
  image_suggestion String?
  created_at       DateTime?  @default(now()) @db.Timestamptz(6)
  social_posts     SocialPost @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_variations_post_id_fkey")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_sheets_sync_configs {
  id             String    @id(map: "sheets_sync_configs_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id        String?   @db.Uuid
  spreadsheet_id String
  sheet_name     String
  sync_type      String
  range          String?
  is_active      Boolean?  @default(true)
  last_synced_at DateTime? @db.Timestamptz(6)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([user_id, spreadsheet_id, sheet_name, sync_type], map: "sheets_sync_configs_user_id_spreadsheet_id_sheet_name_sync__key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_site_content {
  id         String    @id(map: "site_content_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  page       String
  section    String
  key        String
  content    String
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_by String?   @db.Uuid
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([page, section, key], map: "site_content_page_section_key_key")
  @@index([key], map: "idx_site_content_key")
  @@index([page, section], map: "idx_site_content_page_section")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_sync_logs {
  id               String    @id(map: "sync_logs_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String?   @db.Uuid
  integration_name String
  sync_type        String
  status           String
  items_synced     Int?      @default(0)
  error_message    String?
  started_at       DateTime? @default(now()) @db.Timestamptz(6)
  completed_at     DateTime? @db.Timestamptz(6)
  metadata         Json?
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_system_backups {
  id            String    @id(map: "system_backups_pkey")
  status        String?   @default("pending")
  size          String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  completed_at  DateTime? @db.Timestamptz(6)
  error_message String?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_system_settings {
  id               String    @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  key              String    @unique(map: "system_settings_key_key")
  value            Json?
  maintenance_mode Boolean?  @default(false)
  ai_enabled       Boolean?  @default(true)
  banner_message   String?
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_tasks {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  client_id   String?   @db.Uuid
  assigned_to String?   @db.Uuid
  title       String
  description String?
  due_date    DateTime  @db.Date
  priority    String?   @default("medium")
  status      String?   @default("todo")
  type        String?   @default("general")
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@index([assigned_to], map: "idx_social_tasks_assigned_to")
  @@index([client_id], map: "idx_social_tasks_client_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_team_comments {
  id                  String              @id(map: "team_comments_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  team_member_id      String              @db.Uuid
  text                String
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  social_team_members social_team_members @relation(fields: [team_member_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "team_comments_team_member_id_fkey")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_team_member_clients {
  team_member_id      String              @db.Uuid
  client_id           String              @db.Uuid
  social_team_members social_team_members @relation(fields: [team_member_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "team_member_clients_team_member_id_fkey")

  @@id([team_member_id, client_id], map: "team_member_clients_pkey")
  @@index([client_id, team_member_id], map: "idx_team_member_clients_client_team")
  @@index([team_member_id], map: "idx_team_member_clients_team_member_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_team_members {
  id                         String                       @id(map: "team_members_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id                    String?                      @unique(map: "team_members_user_id_key") @db.Uuid
  name                       String
  role                       String
  member_type                String
  avatar                     String?
  active_tasks_count         Int?                         @default(0)
  capacity_score             Int?                         @default(0)
  hourly_rate                Decimal?                     @db.Decimal(10, 2)
  monthly_salary             Decimal?                     @db.Decimal(10, 2)
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  email                      String?
  plan                       String?                      @default("free")
  trial_start_date           DateTime?                    @db.Timestamptz(6)
  trial_days                 Int?                         @default(30)
  subscription_status        String?
  subscription_start_date    DateTime?                    @db.Timestamptz(6)
  organization_id            String?                      @db.Uuid
  social_activity_logs       social_activity_logs[]
  social_post_comments       social_post_comments[]
  social_team_comments       social_team_comments[]
  social_team_member_clients social_team_member_clients[]
  social_organizations       social_organizations?        @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "team_members_organization_id_fkey")

  @@index([email], map: "idx_team_members_email")
  @@index([subscription_status], map: "idx_team_members_subscription_status")
  @@index([trial_start_date], map: "idx_team_members_trial_start_date")
  @@index([user_id], map: "idx_team_members_user_id")
  @@index([organization_id], map: "team_members_organization_id_idx")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_user_update_views {
  id                 String             @id(map: "user_update_views_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id            String             @db.Uuid
  update_id          String             @db.Uuid
  viewed_at          DateTime?          @default(now()) @db.Timestamptz(6)
  social_app_updates social_app_updates @relation(fields: [update_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_update_views_update_id_fkey")

  @@unique([user_id, update_id], map: "user_update_views_user_id_update_id_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_users {
  id                   String                @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  clerk_user_id        String                @unique(map: "users_clerk_user_id_key")
  email                String?
  full_name            String?
  avatar_url           String?
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?             @default(now()) @db.Timestamptz(6)
  organization_id      String?               @db.Uuid
  role                 String?               @default("team_member")
  allowed_modules      String[]              @default(["nexus", "client"])
  social_organizations social_organizations? @relation(fields: [organization_id], references: [id], onUpdate: NoAction, map: "users_organization_id_fkey")

  @@index([clerk_user_id], map: "idx_users_clerk_user_id")
  @@index([organization_id], map: "users_organization_id_idx")
  @@index([role], map: "users_role_idx")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_webhook_configs {
  id                String    @id(map: "webhook_configs_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id           String?   @db.Uuid
  integration_name  String
  webhook_url       String
  secret_key        String?
  events            String[]
  is_active         Boolean?  @default(true)
  last_triggered_at DateTime? @db.Timestamptz(6)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  updated_at        DateTime? @default(now()) @db.Timestamptz(6)
}

model system_invitation_links {
  id               String    @id(map: "invitation_links_pkey") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  token            String    @unique(map: "invitation_links_token_key")
  client_id        String?   @db.Uuid
  created_by       String?   @db.Uuid
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  expires_at       DateTime? @db.Timestamptz(6)
  used_at          DateTime? @db.Timestamptz(6)
  is_used          Boolean?  @default(false)
  is_active        Boolean?  @default(true)
  ceo_name         String?
  ceo_email        String?
  ceo_phone        String?
  company_name     String?
  company_logo     String?
  company_address  String?
  company_website  String?
  additional_notes String?
  source           String?   @default("manual")
  metadata         Json?     @default("{}")
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@index([token], map: "idx_invitation_links_token")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model system_settings {
  id           String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id    String?   @unique @db.Uuid
  system_flags Json?     @default("{}")
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)

  @@index([system_flags], map: "idx_system_settings_system_flags", type: Gin)
  @@index([tenant_id], map: "idx_system_settings_tenant_id")
}

enum client_status {
  Active
  Paused
  Archived
  Pending_Payment @map("Pending Payment")
  Onboarding
  Overdue
}

enum member_type {
  employee
  freelancer
}

enum onboarding_status {
  invited
  completed
}

enum post_status {
  draft
  internal_review
  scheduled
  published
  failed
  pending_approval
}

enum pricing_plan {
  starter
  pro
  agency
  custom
}

enum social_platform {
  facebook
  instagram
  linkedin
  tiktok
  twitter
  google
  whatsapp
  threads
  youtube
  pinterest
  portal
}

enum user_role {
  admin
  account_manager
  content_creator
  designer
  client
}


