# 🏗️ ארכיטקטורה טכנית — Misrad-AI

## Stack טכנולוגי מלא

### Frontend

| טכנולוגיה | תפקיד | גרסה |
|-----------|--------|-------|
| **Next.js** | React Framework, SSR, App Router | 16 |
| **React** | UI Library, Server Components | 19 |
| **TypeScript** | שפה מוקלדת, בטיחות קוד | 5.x |
| **TailwindCSS** | Utility-first CSS, Responsive | 3.x |
| **Framer Motion** | אנימציות, Drag & Drop | 11.x |
| **Lucide React** | 1000+ אייקונים | latest |
| **Recharts** | גרפים ודוחות | 2.x |

### Backend

| טכנולוגיה | תפקיד | גרסה |
|-----------|--------|-------|
| **Next.js Server Actions** | לוגיקה עסקית ('use server') | 16 |
| **Prisma** | ORM, Type-safe queries, Migrations | 6.x |
| **PostgreSQL** | Database ארגוני, RLS, JSONB | 16 |
| **Clerk** | Authentication, 2FA, SSO | latest |
| **Supabase Storage** | אחסון קבצים, S3-compatible | — |

### AI & שירותים

| טכנולוגיה | תפקיד |
|-----------|--------|
| **Google Gemini 2.0 Flash** | מנוע AI ראשי |
| **Vercel AI SDK** (@ai-sdk) | Streaming, Tool calling |
| **Resend** | מיילים טרנזקציוניים |
| **Web Push** | התראות דפדפן (Service Worker) |
| **Svix** | Webhooks עם Delivery Guarantee |

### Deployment

| טכנולוגיה | תפקיד |
|-----------|--------|
| **Vercel / Netlify** | Hosting, CDN עולמי, SSL |
| **Serverless** | Auto-scaling, Zero maintenance |
| **Edge Functions** | Low-latency, Geographic distribution |

---

## Multi-Tenant — איך עובד הבידוד?

### הרעיון

```
PostgreSQL אחד
├── Organization A → רואה רק את הנתונים שלה
├── Organization B → רואה רק את הנתונים שלה
└── Organization C → רואה רק את הנתונים שלה
```

**כל ארגון = tenant נפרד.** אי אפשר לראות נתונים של ארגון אחר.

### 4 שכבות בידוד

#### שכבה 1 — Organization ID בכל טבלה

```sql
-- כל טבלה עסקית מכילה עמודת organization_id
CREATE TABLE nexus_tasks (
  id              UUID PRIMARY KEY,
  organization_id UUID NOT NULL,   -- 👈 חובה!
  title           TEXT NOT NULL,
  ...
);
CREATE INDEX idx_tasks_org ON nexus_tasks(organization_id);
```

#### שכבה 2 — Tenant Guard (Middleware)

```typescript
// lib/prisma-tenant-guard.ts
// כל query עובר דרך middleware שמוסיף WHERE organization_id = ?

export async function getTasks() {
  const orgId = await getOrganizationId(); // מה-Session
  
  return prisma.nexusTask.findMany({
    where: { organizationId: orgId } // 👈 אוטומטי, לא אפשר לעקוף
  });
}
```

#### שכבה 3 — Row Level Security (PostgreSQL)

```sql
-- אפילו SQL ישיר עובר דרך RLS
CREATE POLICY "tasks_tenant_isolation"
  ON nexus_tasks FOR ALL
  USING (organization_id = current_setting('app.organization_id')::uuid);

ALTER TABLE nexus_tasks ENABLE ROW LEVEL SECURITY;
```

#### שכבה 4 — API Guards + Audit

```typescript
'use server';
export async function createTask(data: TaskInput) {
  const session = await auth();          // 1. מי אתה?
  const orgId = await getOrgId();        // 2. לאיזה ארגון?
  await checkPermission('create_task');  // 3. מותר לך?
  await checkRateLimit('create_task');   // 4. לא יותר מדי?
  
  const task = await prisma.nexusTask.create({
    data: { ...data, organizationId: orgId } // 5. orgId מאולץ
  });
  
  await logAudit('task.create', task.id);  // 6. תיעוד
  return task;
}
```

---

## Server Actions — איך זה עובד?

### מהם Server Actions?

פונקציות TypeScript שרצות **אך ורק בשרת**. הקוד לא נשלח לדפדפן.

```typescript
// ✅ Server Action — רץ בשרת
// app/actions/leads.ts
'use server';

export async function createLead(name: string, phone: string) {
  // הלקוח לא רואה את הקוד הזה!
  const orgId = await getOrganizationId();
  return prisma.systemLead.create({
    data: { fullName: name, phone, organizationId: orgId }
  });
}

// ✅ Client Component — רץ בדפדפן
// components/LeadForm.tsx
'use client';
import { createLead } from '@/app/actions/leads';

export function LeadForm() {
  async function onSubmit(formData: FormData) {
    await createLead(formData.get('name'), formData.get('phone'));
  }
  return <form action={onSubmit}>...</form>;
}
```

### למה עדיף מ-REST API?

| REST API (ישן) | Server Actions (Misrad-AI) |
|-----------------|---------------------------|
| Client → HTTP → API Route → DB | Client → Server Action → DB |
| צריך ליצור endpoint לכל פעולה | פונקציה רגילה עם `'use server'` |
| Types לא משותפים | TypeScript מקצה לקצה |
| Error handling ידני | Error handling אוטומטי |

---

## Serverless — מה זה אומר?

### ההבדל

```
Traditional VPS:
  שרת רץ 24/7 → ₪200/חודש גם בלי משתמשים
  
Serverless (Misrad-AI):
  Functions on-demand → ₪0 בלי משתמשים, ₪50 ב-10K בקשות/יום
```

### יתרון ללקוח

- **עלות נמוכה** → אני משלם פחות → אתה משלם פחות
- **Auto-scaling** → 10 או 10,000 משתמשים = אותו ביצוע
- **Zero downtime** → Vercel מנהל הכל

---

## Database Schema — מבנה כללי

**250+ טבלאות** מחולקות ל:

| מודול | טבלאות | דוגמאות |
|-------|--------|---------|
| Nexus | ~40 | nexus_tasks, nexus_users, nexus_time_entries |
| System | ~30 | system_leads, system_call_analysis, system_automations |
| Social | ~50 | social_posts, social_campaigns, social_conversations |
| Finance | ~15 | finance_invoices, finance_expenses |
| Client | ~25 | client_portal_users, client_shared_files, client_workflows |
| Operations | ~35 | operations_work_orders, operations_inventory |
| Shared | ~55 | organizations, billing, audit_logs, ai_usage |

---

## ביצועים

### Database Optimization
- **Connection pooling** — Prisma manages pool
- **Indexes** — על כל `organization_id` + שדות חיפוש
- **Prepared statements** — מהירות query
- **Composite indexes** — `(organization_id, status)`, `(organization_id, created_at)`

### Frontend Optimization
- **Server Components** — HTML מוכן מהשרת
- **Streaming SSR** — תוכן מגיע בזרם
- **Code splitting** — טוען רק מה שצריך
- **Image optimization** — Next.js אופטימיזציה אוטומטית
- **CDN** — קבצים סטטיים מ-Edge קרוב

---

## Scalability

| טווח | סטטוס |
|-------|--------|
| 1-100 לקוחות | ✅ אפס בעיות |
| 100-500 לקוחות | ✅ מוכן |
| 500-1,000 | ⚠️ ייתכנו צווארי בקבוק, צריך monitoring |
| 1,000+ | ❌ צריך שדרוג תשתית |

---

## גיבוי ושחזור

| סוג | תדירות | שמירה |
|-----|---------|-------|
| Database | יומי (02:00 UTC) | 30 יום |
| Storage | continuous | 30 יום |
| Audit Logs | שבועי | 90 יום |

**שחזור:** Point-in-time, עד 30 יום אחורה.

**ייצוא ידני:** SQL / CSV / JSON — בכל רגע מהגדרות.

---

## Migration Path — עזיבה

| אופציה | פרטים |
|--------|--------|
| **Export נתונים** | SQL/CSV/JSON — חינם |
| **Self-Hosted** | קוד מלא + הוראות — ₪5,000 חד-פעמי |
| **Source Code Escrow** | ללקוחות גדולים — לפי הסכם |

---

📖 **המשך:** [מודל נתונים →](./03-מודל-נתונים.md)
