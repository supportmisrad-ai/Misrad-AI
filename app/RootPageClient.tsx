// DEPRECATED: This file is dead code — the root page (app/page.tsx) is now a
// server-rendered landing page. Auth routing goes through /me and /login.
// Kept as empty stub to avoid accidental re-creation.
'use client';

export default function RootPageClient() {
  return null;
}
